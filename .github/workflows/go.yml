name: Go
on: [push]
jobs:

  build:
    name: Build
    runs-on: golang:buster
    steps:

    - name: Check out code into the Go module directory
      uses: actions/checkout@v1

    - name: Build
      run: make all
    
